import { FieldErrorsPipe } from './field-errors.pipe';

describe('FieldErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
